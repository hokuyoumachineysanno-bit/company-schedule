# 社内業務ポータル v0.9.1

## 補助 / 同乗者の時間バー反映
- 移動タスクの「補助 / 同乗者」で社員を複数選択
- 選択した社員の時間バーにも同じ移動区間を自動表示
- 運転者側は 🚐 表示
- 補助側は「↳補助」表示
- 補助人数に固定上限なし
- 移動人時は運転者 + 補助人数 × 移動時間で計算

既存v0.9のデータ構造 passengerIds をそのまま利用するため、旧データも引き継げます。
