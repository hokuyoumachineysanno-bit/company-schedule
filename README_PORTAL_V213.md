# v2.0.13

時間バーのドラッグが開始されない原因を修正。

## 原因
日フォーカスのDOMは `.day-gantt` / `.track` なのに、
ドラッグ処理が存在しない `.gantt` 要素を探していたため、
マウスカーソルだけ変化してドラッグ処理は開始されていませんでした。

## 修正
- 各バーの直接の基準となる `.track` を使用
- 終日 / 午前 / 午後それぞれの表示範囲で30分単位の位置計算
- バー移動、右端伸縮、重複チェックを維持
